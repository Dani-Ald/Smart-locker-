/**
 * eventos.tsx — Ruta Expo Router para la pestaña de Eventos.
 *
 * Delega toda la lógica y UI a EventosScreen para mantener
 * la carpeta app/ solo con routing y sin lógica de negocio.
 */
import EventosScreen from '@/screens/EventosScreen';

export default EventosScreen;
